import React from "react";
import { Switch } from "react-router-dom";

const Routes: React.FC = () => {
  return (
    <Switch>
      {/* <Route path="/" exact component={} />  */}
      {/* <Route path="/" isPrivate component={} /> */}
    </Switch>
  );
};

export default Routes;
